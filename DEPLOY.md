# DEPLOY.md - הרצה מקומית ועדכון השרת החי

קובץ זה מתאר שני תהליכים: איך מריצים את המערכת (כולל הצ'אטבוט) מקומית על המחשב שלך, ואיך
מעדכנים את השרת החי ב-PythonAnywhere אחרי שיש שינוי קוד חדש בריפו. שני התהליכים כתובים כאן
כשלבים קבועים וחוזרים - לא "עשיתי פעם אחת ידנית ושכחתי איך".

## חלק 1: הרצה מקומית

### הרצה ראשונה (רק פעם אחת)

1. ודאו שיש לכם `venv` מותקן (ראו `setup.sh` אם עוד לא):
   ```bash
   bash setup.sh
   ```

2. הגדירו את מפתח ה-Gemini API שלכם. הפרויקט קורא אותו ממשתנה סביבה בשם `GEMINI_API_KEY`,
   ולעולם לא כתוב אותו ישירות בקוד. מקומית זה נעשה דרך קובץ `.env`:
   ```bash
   cp .env.example .env
   ```
   ואז פתחו את `.env` בעורך טקסט (או ב-VS Code) והחליפו את `your_real_api_key_here` במפתח
   האמיתי שלכם (אפשר ליצור מפתח בחינם ב-https://aistudio.google.com/apikey).

   **חשוב:** קובץ `.env` לא נכנס ל-git (הוא כבר רשום ב-`.gitignore`) - כי הוא מכיל סוד אמיתי.
   אל תשתפו את הקובץ הזה עם אף אחד, ואל תדביקו את המפתח האמיתי בשום מקום מלבד קובץ ה-`.env`.

### כל הרצה נוספת

```bash
source venv/bin/activate
python app.py
```

ואז פותחים דפדפן בכתובת `http://localhost:5000`. בסרגל הניווט יש קישור "צ'אט עם הבוט" -
הוא זמין לכל לקוח/ה, בלי צורך בהתחברות ניהול. מסך הניהול עדיין דורש סיסמה (`1111` כברירת מחדל,
ראו `config.py`).

בכל הרצה, האפליקציה גם מזינה אוטומטית דאטת דמו (לקוחות/תורים/לידים לדוגמה) אם היא עוד לא קיימת
- ראו `seed_demo_data.py`. זו הדאטה שמשמשת לבדיקת תרחישי הצ'אטבוט.

## חלק 2: עדכון השרת החי ב-PythonAnywhere

זהו תהליך **ידני** (אין CI/CD אוטומטי בפרויקט הזה) - אבל הוא תהליך קבוע וחוזר, בכל פעם באותם
שלבים:

1. היכנסו לחשבון PythonAnywhere שלכם, ופתחו **Consoles -> Bash** (או השתמשו בקונסולה קיימת).

2. עברו לתיקיית הפרויקט על השרת ומשכו את הקוד העדכני מ-GitHub:
   ```bash
   cd ~/Midterm-Project
   git pull origin main
   ```

3. אם `requirements.txt` השתנה (כמו בעדכון הזה, שמוסיף `python-dotenv` ו-`google-genai`) -
   התקינו את החבילות החדשות בסביבה הוירטואלית של PythonAnywhere:
   ```bash
   pip install -r requirements.txt
   ```

4. הגדירו את משתנה הסביבה `GEMINI_API_KEY` **פעם אחת** ישירות ב-PythonAnywhere (לא דרך `.env` -
   שם הוא לא נטען אוטומטית באותה צורה):
   - לכו ללשונית **Web** בפאנל של PythonAnywhere.
   - גללו למטה לאזור **Environment variables**.
   - הוסיפו משתנה בשם `GEMINI_API_KEY` עם הערך האמיתי של המפתח שלכם.
   - שמרו.

   (זה נדרש פעם אחת בלבד - המשתנה נשאר שמור גם בעדכונים הבאים, אלא אם תמחקו אותו.)

5. הפעילו מחדש את האפליקציה: בלשונית **Web**, לחצו על הכפתור הירוק **Reload**
   (`maorshapira.pythonanywhere.com`).

6. ודאו שהעדכון עלה בהצלחה: פתחו את האתר החי, ובדקו שהתאריך/שינוי האחרון אכן מופיע (למשל,
   נסו את הקישור "צ'אט עם הבוט" בסרגל הניווט - אם הוא מופיע, השרת רץ על הגרסה העדכנית).

### מה עושים אם ה-Reload נכשל / יש שגיאה

בלשונית **Web** יש קישור ל-**Error log** ו-**Server log** - שם רואים את השגיאה המדויקת.
הסיבות הכי נפוצות: שכחתם להריץ `pip install -r requirements.txt` אחרי הוספת חבילה חדשה,
או ששכחתם להגדיר את `GEMINI_API_KEY` בסביבה (שגיאה שתופיע רק כשמנסים להשתמש בצ'אטבוט
בפועל, לא בטעינת האתר עצמו - ראו `chatbot/gemini_client.py`).
